<?php
 if ( ! defined( 'ABSPATH' ) ) { exit; } function hvm_licensed_domains() { $list = defined( 'HVM_LICENSED_DOMAINS' ) ? (array) HVM_LICENSED_DOMAINS : array(); if ( 1 === count( $list ) && is_string( reset( $list ) ) ) { $list = explode( ',', reset( $list ) ); } $list = array_filter( array_map( function ( $d ) { return preg_replace( '/^www\./', '', strtolower( trim( (string) $d ) ) ); }, $list ) ); return apply_filters( 'hvm_licensed_domains', $list ); } function hvm_is_licensed() { $domains = hvm_licensed_domains(); if ( empty( $domains ) ) { return true; } $host = (string) wp_parse_url( home_url(), PHP_URL_HOST ); $host = preg_replace( '/^www\./', '', strtolower( $host ) ); if ( preg_match( '/(localhost|\.test|\.local|\.staging|\.ddev\.site|\.wpengine\.com)$/', $host ) ) { return true; } return in_array( $host, $domains, true ); } function hvm_license_enforced() { return defined( 'HVM_LICENSE_ENFORCE' ) ? (bool) HVM_LICENSE_ENFORCE : false; } if ( hvm_license_enforced() && ! hvm_is_licensed() ) { add_filter( 'hvm_mixer_widget_classes', '__return_empty_array', 9999 ); } add_action( 'admin_notices', function () { if ( hvm_is_licensed() || ! current_user_can( 'manage_options' ) ) { return; } $enforced = hvm_license_enforced(); echo '<div class="notice notice-' . ( $enforced ? 'error' : 'warning' ) . '"><p><strong>DoitDigital Theme:</strong> this domain is not licensed for this theme. ' . ( $enforced ? 'The theme\'s custom widgets are disabled here. ' : '' ) . 'Contact the developer to license this domain.</p></div>'; } ); 