# Build output + the cached dist-repo clone used by release.sh
build/dist/
build/.dist-repo/

# OS / editor cruft
.DS_Store
Thumbs.db
*.map

# Composer: keep vendor/ (the update-checker library must ship with the theme)
# so it is intentionally NOT ignored.
